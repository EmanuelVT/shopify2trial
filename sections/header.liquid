{% case section.settings.header_type%}
{% when 'header1' %}
{{ 'Header1.css' | asset_url | stylesheet_tag }}

<!--
<header class="{%-if section.settings.sticky == false and section.settings.header_background == "transparent" -%}header header1 transparent-static
{% elsif section.settings.sticky == true and section.settings.header_background == "white" %} header header1 white-fixed
{% elsif section.settings.sticky == true and section.settings.header_background == "transparent" %} header header1 transparent-fixed
{% else %}
header header1
{% endif %}">-->

<header
  class="header header1 {% if section.settings.header_background == "transparent"%}  trans  {% endif %} {% if section.settings.static == false %} transparent-fixed {% endif %} {% if section.settings.header_background == "white" %} chosen-white {% endif %}">

  <ul class="header1__collections-nav">
    {%- if section.settings.menu != blank -%}
    {%- for link in section.settings.menu.links -%}
    {%- if link.links == blank -%}
    <li class="link-1"> <a href="{{ link.url }}">{{ link.title | escape }}</a> </li>
    {%- else -%}
    <li class="link-1">
      <a href="{{ link.url }}">{{ link.title | escape }}</a>
      <ul class="header1__sub-links">
        {%- for childlink in link.links -%}
        <li><a href="{{ childlink.url }}">{{ childlink.title | escape }}</a></li>
        {%- endfor -%}
      </ul>
    </li>
    {%- endif -%}
    {%- endfor -%}

    {%- endif -%}
  </ul>
  <div class="header1__logo">
    <a href="{{ routes.root_url }}">
      {%- if section.settings.logo != blank -%}
      {%- assign image_size = section.settings.logo_width | append: 'x' -%}
      <img
        srcset="{{ section.settings.logo | img_url: image_size }} 1x, {{ section.settings.logo | img_url: image_size, scale: 2 }} 2x"
        src="{{ section.settings.logo | img_url: image_size }}" loading="lazy" class="header__heading-logo"
        width="{{ section.settings.logo.width }}" height="{{ section.settings.logo.height }}"
        alt="{{ section.settings.logo.alt | default: shop.name | escape }}">
      {%- else -%}
      {{ shop.name }}
      {%- endif -%}
    </a>
  </div>
  <ul class="header1__collections-nav">
    {%- if section.settings.menu2 != blank -%}
    {%- for link in section.settings.menu2.links -%}
    {%- if link.links == blank -%}
    <li class="link-4"><a href="{{ link.url }}">{{ link.title | escape }}</a></li>
    {%-else-%}
    <li class="link-4">
      <a href="{{ link.url }}">{{ link.title | escape }}</a>
      <ul class="header1__sub-links">
        {%- for childlink in link.links -%}
        <li><a href="{{ childlink.url }}">{{ childlink.title | escape }}</a></li>
        {%- endfor -%}
      </ul>
    </li>
    {%- endif -%}
    {%-endfor-%}
    {%- endif -%}
    <a href="/cart">
      <i class="lni lni-cart"></i>
    </a>
    {%- if shop.customer_accounts_enabled -%}
    <a href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}"
      class="header__icon header__icon--account link link--text focus-inset{% if section.settings.menu != blank %} small-hide{% endif %}">
      {% render 'icon-account' %}
      <span class="visually-hidden">
        {%- liquid
      if customer
        echo 'customer.account_fallback' | t
      else
        echo 'customer.log_in' | t
      endif
    -%}
      </span>
    </a>
    {%- endif -%}

    <a href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}">
      <i class="lni lni-user"></i></a>

  </ul>
</header>

{% when 'header2' %}
{{ 'Header2.css' | asset_url | stylesheet_tag }}
<header
  class="header header2 {% if section.settings.header_background == "transparent"%}  trans  {% endif %} {% if section.settings.static == false %} transparent-fixed {% endif %} {% if section.settings.header_background == "white" %} chosen-white {% endif %}">
  <div class="header2__logo">
    <a href="{{ routes.root_url }}">
      {%- if section.settings.logo != blank -%}
      {%- assign image_size = section.settings.logo_width | append: 'x' -%}
      <img
        srcset="{{ section.settings.logo | img_url: image_size }} 1x, {{ section.settings.logo | img_url: image_size, scale: 2 }} 2x"
        src="{{ section.settings.logo | img_url: image_size }}" loading="lazy" class="header__heading-logo"
        width="{{ section.settings.logo.width }}" height="{{ section.settings.logo.height }}"
        alt="{{ section.settings.logo.alt | default: shop.name | escape }}">
      {%- else -%}
      {{ shop.name }}
      {%- endif -%}
    </a>
  </div>

  <ul class="header2__collections-nav">
    {%- if section.settings.menu != blank -%}
    {%- for link in section.settings.menu.links -%}
    {%- if link.links == blank -%}
    <li class="link-1"> <a href="{{ link.url }}">{{ link.title | escape }}</a> </li>
    {%- else -%}
    <li class="link-1">
      <a href="{{ link.url }}">{{ link.title | escape }}</a>
      <ul class="header2__sub-links">
        {%- for childlink in link.links -%}
        <li><a href="{{ childlink.url }}">{{ childlink.title | escape }}</a></li>
        {%- endfor -%}
      </ul>
    </li>
    {%- endif -%}
    {%- endfor -%}

    {%- endif -%}
  </ul>

  <div class="header2__collections-nav">
    <a href="/cart">
      <i class="lni lni-cart"></i>
    </a>
    {%- if shop.customer_accounts_enabled -%}
    <a href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}"
      class="header__icon header__icon--account link link--text focus-inset{% if section.settings.menu != blank %} small-hide{% endif %}">
      {% render 'icon-account' %}
      <span class="visually-hidden">
        {%- liquid
      if customer
        echo 'customer.account_fallback' | t
      else
        echo 'customer.log_in' | t
      endif
    -%}
      </span>
    </a>
    {%- endif -%}

    <a href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}">
      <i class="lni lni-user"></i></a>
  </div>



</header>

{% when 'header3' %}
{{ 'Header3.css' | asset_url | stylesheet_tag }}
<!--<header class="{%-if section.settings.sticky == false and section.settings.header_background == "transparent" -%}header header3 transparent-static
    {% elsif section.settings.sticky == true and section.settings.header_background == "white" %} header header3 white-fixed
    {% elsif section.settings.sticky == true and section.settings.header_background == "transparent" %} header header3 transparent-fixed
    {% else %}
    header header3
 {% endif %}">-->

<header
  class="header header3  {% if section.settings.header_background == "transparent"%}  trans  {% endif %} {% if section.settings.static == false %} transparent-fixed {% endif %} {% if section.settings.header_background == "white" %} chosen-white {% endif %} "
  id="header3">
  <div class="header3__logo">
    <a href="{{ routes.root_url }}">
      {%- if section.settings.logo != blank -%}
      {%- assign image_size = section.settings.logo_width | append: 'x' -%}
      <img
        srcset="{{ section.settings.logo | img_url: image_size }} 1x, {{ section.settings.logo | img_url: image_size, scale: 2 }} 2x"
        src="{{ section.settings.logo | img_url: image_size }}" loading="lazy" class="header__heading-logo"
        width="{{ section.settings.logo.width }}" height="{{ section.settings.logo.height }}"
        alt="{{ section.settings.logo.alt | default: shop.name | escape }}">
      {%- else -%}
      {{ shop.name }}
      {%- endif -%}
    </a>
  </div>

  <ul class="header3__collections-nav">
    {%- if section.settings.menu != blank -%}
    {%- for link in section.settings.menu.links -%}
    {%- if link.links == blank -%}
    <li class="link-1"> <a href="{{ link.url }}">{{ link.title | escape }}</a></li>
    {%- else -%}
    <li class="link-1"><a href="{{ link.url }}">{{ link.title | escape }}</a>
      <ul class="header3__sub-links">
        {%- for childlink in link.links -%}
        <li><a href="{{ childlink.url }}">{{ childlink.title | escape }}</a></li>
        {%- endfor -%}
      </ul>
    </li>
    {%- endif -%}
    {%- endfor -%}

    {%- endif -%}
    <a href="/cart">
      <i class="lni lni-cart"></i>
    </a>
    {%- if shop.customer_accounts_enabled -%}
    <a href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}"
      class="header__icon header__icon--account link link--text focus-inset{% if section.settings.menu != blank %} small-hide{% endif %}">
      {% render 'icon-account' %}
      <span class="visually-hidden">
        {%- liquid
      if customer
        echo 'customer.account_fallback' | t
      else
        echo 'customer.log_in' | t
      endif
    -%}
      </span>
    </a>
    {%- endif -%}

    <a href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}">
      <i class="lni lni-user"></i></a>
  </ul>
</header>


{% endcase %}
<!--
<script>
  console.log('loaded js from header')
  //Get body element
  const bodyTag = document.getElementsByTagName('body')[0];
  const headerWrapper = document.getElementById('shopify-section-header');
  const header3 = document.getElementById('header3');
  console.log(header3)
  //Check for class fixed-nav

  if (bodyTag.classList.contains('fixed-nav') && bodyTag.classList.contains('transparent-nav')) {
    headerWrapper.style.position = 'fixed';
    headerWrapper.style.top = 0;
    headerWrapper.style.left = 0;
    headerWrapper.style.width = '100%';
    headerWrapper.style.zIndex = '7';

    //remove class of transparent bg when scrolled
    const onScroll = () => {
      const scrollLocation = document.documentElement.scrollTop;

      if (scrollLocation > 50) {
        bodyTag.classList.remove('transparent-nav');
      } else {
        bodyTag.classList.add('transparent-nav');
      }
    }

    window - addEventListener('scroll', onScroll);
  }
  //if class fixed-nav exists remove transparent-nav class when you scroll.
</script>-->

<script src="{{ 'zepto.min.js' | asset_url}}"></script>



<script>
  /*
  //Get body element
  const bodyTag = $('body');
  const headerWrapper = $('.header3');
  //Check for class fixed-nav
 
  if (bodyTag.hasClass('fixed-nav')) {
    headerWrapper.css('position', 'fixed');
    headerWrapper.css('top', 'initial');
    headerWrapper.css('left', '0');
    headerWrapper.css('width', '100%');
    headerWrapper.css('z-index', '7');





    if (bodyTag.hasClass('transparent-nav')) {
      //remove class of transparent bg when scrolled
      const onScroll = () => {
        const scrollLocation = document.documentElement.scrollTop;

        if (scrollLocation > 5) {
          bodyTag.removeClass('transparent-nav');
        } else {
          bodyTag.addClass('transparent-nav');
        }
      }

      window - addEventListener('scroll', onScroll);
    }


  }*/
</script>

<!--
<script>
  function headerSize() {
    var $headerHeight = $('div#shopify-section-header').outerHeight();
    $('#PageContainer').css('padding-top', $headerHeight);
  }

  function debounce(fn, delay) {
    var timer = null;
    return function () {
      var context = this,
        args = arguments;
      clearTimeout(timer);
      timer = setTimeout(function () {
        fn.apply(context, args);
      }, delay);
    };
  }

  $(window).on("load", headerSize);
  $(window).on("resize", debounce(500, headerSize));

  $(window).scroll(function () {
    scroll = $(window).scrollTop();
    if (scroll >= 1) {
      $('.fixed-nav').css('top', '0');
    } else {
      $('.fixed-nav').css('top', '5');
    }
  });-->
</script>

{% schema %}
{
"name": "t:sections.header.name",
"settings": [
{
"type": "select",
"id": "header_type",
"label": "Type of Header",
"options": [
{
"value": "header1",
"label": "Header 1"
},
{
"value": "header2",
"label": "Header 2"
},
{
"value": "header3",
"label": "Header 3"
}
],
"default": "header3"
},

{
"type": "image_picker",
"id": "logo",
"label": "t:sections.header.settings.logo.label"
},
{
"type": "range",
"id": "logo_width",
"min": 50,
"max": 250,
"step": 10,
"default": 100,
"unit": "t:sections.header.settings.logo_width.unit",
"label": "t:sections.header.settings.logo_width.label"
},
{
"type": "select",
"id": "logo_position",
"options": [
{
"value": "middle-left",
"label": "t:sections.header.settings.logo_position.options__1.label"
},
{
"value": "top-left",
"label": "t:sections.header.settings.logo_position.options__2.label"
},
{
"value": "top-center",
"label": "t:sections.header.settings.logo_position.options__3.label"
}
],
"default": "middle-left",
"label": "t:sections.header.settings.logo_position.label"
},
{
"type": "link_list",
"id": "menu",
"default": "main-menu",
"label": "t:sections.header.settings.menu.label"
},
{
"type": "link_list",
"id": "menu2",
"default": "main-menu",
"label": "Alternate Menu"
},
{
"type": "checkbox",
"id": "static",
"label": "Static header",
"default": false
},
{
"type": "radio",
"id": "header_background",
"label": "Header Background Type",
"options": [
{
"value": "transparent",
"label": "Transparent"
},
{
"value": "white",
"label": "Whiteyys"
}
],
"default": "transparent"
}
]
}
{% endschema %}